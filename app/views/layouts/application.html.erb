<!DOCTYPE html>
<html>
<head>
  <title>Wikiful</title>
    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
  <![endif]-->

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
  <%= bootstrap_flash %>
  <div class="nav navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="navbar-header">
	  <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#collapse">
		<a>Menu</a>
	  </button>
	</div>
    <div class="collapse navbar-collapse" id="collapse">  
      <div class="container">
	    <a class="navbar-brand">Wikiful</a>
        <div class="btn-group"> 
          <%= link_to "Home", root_path, class: is_active?(root_path, "brand btn btn-default navbar-btn") %>        
          <%= link_to "Articles", articles_path, class: is_active?(articles_path, "btn btn-default navbar-btn") %>
          <%= link_to "Categories", categories_path, class: is_active?(categories_path, "btn btn-default navbar-btn") %>
          <button class="btn btn-default navbar-btn dropdown-toggle" type="button" data-toggle="dropdown">     
      	    About <span class="caret"></span> 
      	  </button>
      	  <div class="dropdown-menu pull-right" role="menu">
      		<p>
      	      Wikiful is yet another wiki.</br>
      	      If you are unhappy about it's</br> 
      	      lack of features you are welcome to</br>
      	      take it and get forked.
      		</p>
      	  </div>
        </div>
        <div class="btn-group">
      	  <%= link_to "New", new_article_path, class: is_active?(new_article_path, "btn btn-default navbar-btn") %>
      	</div>
        <div class="navbar-right">
          <% if current_user %>
            <p class="navbar-text"><%= current_user.email %></p>
            <%= link_to "Log Out", logout_path, :class => 'btn btn-default navbar-btn' %>
          <% else %>
            <div class="btn-group">
              <%= link_to "Sign Up", signup_path, class: is_active?(signup_path, "btn btn-default navbar-btn") %>
              <%= link_to "Log In", login_path, class: is_active?(login_path, "btn btn-default navbar-btn") %>
            </div>
          <% end %>
	    </div>
      </div>
    </div>
  </div>                  <!-- end of NavBar -->
  <div class="container">
    <%= yield %>	
  </div>
</body>
</html>
